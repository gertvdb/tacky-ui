// Example of a test file
// Jest will pick up any file with the .test.ts extension and run the tests inside it.

describe("Routing", () => {
  test("Test Suite setup", () => {
    expect(true).toBe(true);
  });
});
